{% extends 'base.html.twig' %}

{% block body %}
<div class="row">
    <div class="col-lg-12 text-center">
        <a href="{{ path('tabletennis_game_new') }}" class="btn btn-info" role="button">Report a new table tennis game</a>
    </div>
    <div class="col-lg-12 text-center">
        <a href="{{ path('tabletennis_game_new') }}" class="btn btn-info" role="button">Report a new pool game</a>
    </div>
</div>

{% endblock %}
