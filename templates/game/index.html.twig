{% extends 'base.html.twig' %}

{% block title %}Game index{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-lg-12 text-center">
            <h1 class="mt-5">Games</h1>
            <a href="{{ path('game_new') }}" class="btn btn-info" role="button">Add a new game</a>
        </div>
    </div>


<div class="row">
    <div class="col-lg-12 text-center">
    {% include "/game/table.html.twig" %}

    <a href="{{ path('game_new') }}" class="btn btn-info" role="button">Add a new game</a>
    </div>
</div>
{% endblock %}
